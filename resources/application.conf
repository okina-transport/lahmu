ktor {
    deployment {
        port = 8080
        port = ${?PORT}
        watch = [ lahmu ]
    }
    application {
        modules = [ org.entur.lahmu.config.ApplicationConfigKt.mainModule ]
    }
}
